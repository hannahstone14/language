specVersion: "v1"
serviceId: "hangul-helper"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "hangul-helper-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
